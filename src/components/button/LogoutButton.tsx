import React from 'react'
import Button from './Button'

const LogoutButton = () => {
  const logout = () => {
    // atk와 rtk값 삭제하기
  }
  return (
    <Button>
      로그아웃
    </Button>
  )
}

export default LogoutButton